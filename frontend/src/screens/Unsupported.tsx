import { Camera, ClipboardList, ImageOff, RotateCcw } from 'lucide-react';
type Props = { kind: 'image_unclear' | 'no_candidates'; message: string; onImage: () => void; onManual: () => void };
export function Unsupported({ kind, message, onImage, onManual }: Props) { const unclear = kind === 'image_unclear'; return <section className="card empty-state"><span className="empty-icon">{unclear ? <ImageOff/> : <Camera/>}</span><p className="eyebrow">{unclear ? 'Image unclear' : 'No candidate found'}</p><h2>{unclear ? 'We need a clearer view' : 'Nothing legible was detected'}</h2><p>{message}</p><div className="empty-actions"><button className="button button-secondary" onClick={onManual}><ClipboardList size={15}/>Enter manually</button><button className="button button-primary" onClick={onImage}><RotateCcw size={15}/>Upload another image</button></div></section>; }
