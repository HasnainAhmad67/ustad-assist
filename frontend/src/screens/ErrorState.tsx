import { AlertCircle, ArrowLeft, RotateCcw } from 'lucide-react';
export function ErrorState({ message, onRetry, onReset }: { message: string; onRetry: () => void; onReset: () => void }) { return <section className="card empty-state"><span className="empty-icon"><AlertCircle/></span><p className="eyebrow">Connection issue</p><h2>The workflow paused safely</h2><p>{message}</p><div className="empty-actions"><button className="button button-secondary" onClick={onReset}><ArrowLeft size={15}/>Start over</button><button className="button button-primary" onClick={onRetry}><RotateCcw size={15}/>Retry request</button></div></section>; }
