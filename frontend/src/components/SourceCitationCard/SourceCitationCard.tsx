import { ExternalLink, FileText } from 'lucide-react';
import type { SourceCitationOut } from '../../types/api';
export function SourceCitationCard({ source }: { source: SourceCitationOut }) { return <section className="card card-pad citation"><div className="section-title"><FileText size={17}/><h3>Source</h3></div><div className="citation-row"><span className="citation-label">Manual title</span><strong>{source.manual_title}</strong></div>{source.manual_document_number && <div className="citation-row"><span className="citation-label">Document no.</span><span className="mono">{source.manual_document_number}</span></div>}{source.page && <div className="citation-row"><span className="citation-label">Section / page</span><span className="mono">{source.page}</span></div>}{source.official_url && <div className="citation-row"><span className="citation-label">Official link</span><a href={source.official_url} target="_blank" rel="noreferrer">Open source <ExternalLink size={13} /></a></div>}</section>; }
