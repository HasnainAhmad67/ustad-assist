import { HardHat, ShieldAlert, ShieldCheck } from 'lucide-react';
import type { SafetyOut } from '../../types/api';
export function SafetyCard({ safety }: { safety?: SafetyOut | null }) { if (!safety) return null; if (safety.escalate) return <section className="notice notice-danger" aria-live="assertive"><ShieldAlert size={22}/><div><strong>Safety escalation required</strong><div>{safety.warning_text || 'Stop work and follow your site escalation procedure.'}</div>{safety.reason_category && <small>Reason category: <span className="mono">{safety.reason_category}</span></small>}</div></section>; if (safety.technician_only) return <section className="notice notice-caution"><HardHat size={20}/><div><strong>Technician-only checks apply</strong><div>{safety.warning_text || 'Only a qualified technician should perform energized or enclosure-level checks.'}</div></div></section>; return <section className="notice notice-success"><ShieldCheck size={20}/><div><strong>Safety check</strong><div>{safety.warning_text || 'Follow the safe checks below and stop if conditions differ from the documented procedure.'}</div></div></section>; }
