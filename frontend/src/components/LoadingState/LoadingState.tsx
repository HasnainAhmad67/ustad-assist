import { CheckCircle2, LoaderCircle, Search, ShieldCheck, Sparkles } from 'lucide-react';
const steps = [{ label: 'Validating equipment identity', icon: CheckCircle2 }, { label: 'Retrieving verified evidence', icon: Search }, { label: 'Preparing grounded guidance', icon: Sparkles }, { label: 'Running safety checks', icon: ShieldCheck }];
export function LoadingState() { return <section className="card loading-card"><p className="eyebrow">Retrieving evidence</p><h2>Building a grounded response</h2><p className="helper">Ustad Assist is checking the verified knowledge base before any guidance is shown.</p>{steps.map((step, index) => <div className={`loading-line ${index === 0 ? 'done' : ''}`} key={step.label}><span className="loader-dot">{index > 0 && <LoaderCircle size={14}/>}</span><span>{step.label}</span></div>)}<p className="loading-foot">Do not disconnect or make equipment changes while the analysis is in progress.</p></section>; }
